language js

JsModuleSource() as $source where {
  $source <: r".\.\./\.\./.*",
  register_diagnostic(span=$source, message="Imports with ../../ are forbidden: use the highest barrel in reach (../index.ts); in tests, #scripts.")
}
